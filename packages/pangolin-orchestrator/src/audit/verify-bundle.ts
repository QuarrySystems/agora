// Moved to @quarry-systems/pangolin-core (single source of truth). Re-export shim.
// checkHandoffClosure stays INTERNAL to the moved file — verifyBundle calls it locally.
export { verifyBundle } from '@quarry-systems/pangolin-core';
